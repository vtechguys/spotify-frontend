import React from 'react';

export const DropDown = (props) =>{
    return (
        <div className="DropDown">
            {
                // props.dropDownList.map(list=>{
                //     let type = list.type;
                //     <div onClick={(type) =>props.clicked(type)}>
                //         {list.name}
                //     </div>
                // })
            }
        </div>
    )
}